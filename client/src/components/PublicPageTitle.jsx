export function PublicPageTitle({ title }) {
    return (
        <div className="container">
            <div className="row">
                <h1 className="col-12 display-1 mb-5">{title}</h1>
            </div>
        </div>
    );
}